/**
 * This should be valid in TypeScript but anoyingly useless in js.
 * Guess I'll need to migrate codebase to TypeScript sooner than I've thought.
 *
 * @template T
 * @param {T} value
 * @returns {T}
 */
export const cast = (value) => value;
